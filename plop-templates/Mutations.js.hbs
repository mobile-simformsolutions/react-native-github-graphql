import { gql } from 'apollo-boost';

const {{pascalCase name}} = gql`
  mutation {{name}} {
    {{name}}() 
  }
`;

const {{pascalCase name}}Detail = gql`
  mutation {{name}}Detail {
    {{name}}Detail() 
  }
`;

export { {{pascalCase name}}, {{pascalCase name}}Detail };